import * as Icons from "react-bootstrap-icons";

type UserProps = {
  userImage: string;
  userName: string;
  userStar: Number;
};

const UserInfo = (props: UserProps) => {
  return <div></div>;
};

export default UserInfo;

import React from "react";

type EducationProps = {
  startYear: number;
  endYear: number;
};

const EducationYear = (props: EducationProps) => {
  return <div>EducationYear</div>;
};

export default EducationYear;
